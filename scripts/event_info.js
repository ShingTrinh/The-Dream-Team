const backBtn = document.getElementById("returnButton");
backBtn.addEventListener("click", returnClicked);

const join = document.getElementById("joinEventButton");
join.addEventListener("click", joinClicked);

function returnClicked() {
    window.location.replace("volunteer_event_page.html");
}

function joinClicked() {
    // Add user if not already joined event.

}








